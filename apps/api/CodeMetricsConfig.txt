# Throne maintainability thresholds for built-in .NET code metrics analyzers.
# See specs/AGENTS.local.md (Maintainability gate) for governance.
# Cyclomatic complexity (CA1502) and class coupling (CA1506) are owned by the
# .quality maintainability budget (per-method CC ≤10, file fan-out ≤15), so those
# analyzers are off in .editorconfig and intentionally have no thresholds here.

# Maintainability index minimum.
CA1505: 10

# Inheritance depth maximum.
CA1501: 8
